[project]
# The distribution, the command, the module and the repository are all `ypl`.
# uv keys a tool's receipt directory on the distribution name and pyselfupdate
# reads the running version from it, so a distribution named anything else
# installs a `ypl` whose own `update` cannot find itself.
name = "ypl"
version = "2.3.0"
description = "Organize YouTube music playlists of long DJ mixes."
authors = [{ name = "Chris Birch", email = "datapointchris@gmail.com" }]
license = "MIT"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "typer>=0.15.0",
    "rich>=13.0.0",
    "pyselfupdate[typer]>=0.2.2",
    # The write path speaks youtubei directly — see src/ypl/youtubei.py for why
    # it is not the official Data API and not YouTube Music. Reads go through
    # the yt-dlp binary, which is deliberately not a dependency at all — see
    # src/ypl/ytdlp.py.
    # httpx's successor rather than a typosquat of it: same author, developed at
    # pydantic/httpx2 under BSD-3. The name alone reads as one, which is why it is
    # said here.
    "httpx2>=2.9.1",
]

[project.scripts]
ypl = "ypl.main:app"

[dependency-groups]
dev = [
    "pytest>=8.3.3",
    "pytest-cov>=5.0.0",
    "pre-commit>=3.8.0",
    "mypy>=1.11.2",
]

# ---------- Configurations ---------- #

[tool.codespell]
skip = '*.css.map,*.ipynb'
ignore-words-list = "astroid,aNULL,ANULL,anull"
check-filenames = true
# count = ""
# quiet-level = 3
check-hidden = true
builtin = "clear,rare"
uri-ignore-words-list = "*"

[tool.isort]
line_length = 120
profile = "black"
atomic = true
skip = [".gitignore"]
extend_skip = [".md", ".json"]

[tool.mypy]
pretty = true
ignore_missing_imports = true
check_untyped_defs = false
warn_return_any = false

[tool.ruff]
line-length = 140

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "F",    # pyflakes
    "UP",   # pyupgrade
    "B",    # flake8-bugbear
    "SIM",  # flake8-simplify
    "I",    # isort
]
ignore = [
    "SIM108",  # ternary operator — often less readable
]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]
"env.py" = ["F401"]


[tool.ruff.lint.isort]
force-single-line = true
[tool.ruff.format]
quote-style = "single"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"
docstring-code-format = true

[tool.pyright]
typeCheckingMode = "standard"
analyzeUnannotatedFunctions = true

[tool.semantic_release]
version_toml = ["pyproject.toml:project.version"]
branch = "main"
commit_message = "build(release): {version}"
# The package name is hardcoded because python-semantic-release sets no
# variable for it, and `--upgrade-package "$PACKAGE_NAME"` silently no-ops and
# ships a stale lock in the release commit.
build_command = """
    pip install uv
    uv lock --upgrade-package ypl
    git add uv.lock
"""
# The Go API and CLI in api/ and cli/ are not part of this package. A commit
# that changes only their files is not a change to it, and without the filter a
# feat(api) would cut a minor release of it.
commit_parser = "conventional-monorepo"

[tool.semantic_release.commit_parser_options]
path_filters = [".", "!api/**", "!cli/**", "!Taskfile.yml", "!.golangci.yml", "!.sqlfluff"]

[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
verbosity_assertions = 2

[tool.forge]
# Keys the shared toolchain standard owns, written by its generator.
# Dropping one from the template removes it here on the next sync; a key absent
# from this list belongs to the project and is never touched. Do not hand-edit.
managed = [
    ["ruff", "line-length"],
    ["ruff", "format", "quote-style"],
    ["ruff", "format", "indent-style"],
    ["ruff", "format", "skip-magic-trailing-comma"],
    ["ruff", "format", "line-ending"],
    ["ruff", "format", "docstring-code-format"],
    ["ruff", "lint", "select"],
    ["ruff", "lint", "ignore"],
    ["ruff", "lint", "isort", "force-single-line"],
    ["ruff", "lint", "per-file-ignores", "__init__.py"],
    ["mypy", "pretty"],
    ["mypy", "ignore_missing_imports"],
    ["mypy", "check_untyped_defs"],
    ["mypy", "warn_return_any"],
    ["pyright", "typeCheckingMode"],
    ["pyright", "analyzeUnannotatedFunctions"],
    ["codespell", "check-filenames"],
    ["codespell", "check-hidden"],
    ["codespell", "builtin"],
    ["codespell", "uri-ignore-words-list"],
    ["pytest", "ini_options", "verbosity_assertions"],
    ["pytest", "ini_options", "minversion"],
    ["pytest", "ini_options", "testpaths"],
]

[build-system]
requires = ["uv_build>=0.9.13,<0.12"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "ypl"
# The .sql files are the schema, not fixtures — the package cannot open a
# database without them.
source-include = ["src/ypl/*.sql"]
